import pytest
from app import search_blueprint
import json


def test():
    # response = search_blueprint.get('/api/posts/')
    # assert type(response) == list, 'Это не список'
    # assert len(response.json) == 8, 'Ошибка в имени'
